apiVersion: v1
kind: Pod
metadata:
  name: pod-hostpath
  namespace: audit-zone
spec:
  containers:
    - name: nginx
      image: nginx
      volumeMounts:
        - mountPath: /host-data
          name: host-vol
  volumes:
    - name: host-vol
      hostPath:
        path: /tmp
        type: Directory
